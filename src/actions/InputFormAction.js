import { TYPES
} from '../types';

export const typefieldClick = () => {
    return {
        type: TYPES.INPUT_FOCUS_ICONPAD
    };
};

export const numfieldClick = () => {
    return {
        type: TYPES.INPUT_FOCUS_NUMPAD
    };
};
